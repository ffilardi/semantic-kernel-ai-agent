<policies>
    <inbound>
        <base/>
        <set-backend-service backend-id="web-backend" />
        <quota-by-key calls="6000" renewal-period="3600" counter-key="@(context.Subscription?.Key ?? "anonymous")" />
        <rate-limit-by-key calls="100" renewal-period="60" counter-key="@(context.Subscription?.Key ?? "anonymous")" />
    </inbound>
    <backend>
        <base/>
    </backend>
    <outbound>
        <base/>
    </outbound>
    <on-error>
        <base/>
    </on-error>
</policies>